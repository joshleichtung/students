<h1> Editing Student #<%= @student.id %></h1>

<%= form_for(@student) do |f| %>
  <p> 
    <label>
      First Name:
    </label>
    <%= f.text_field :first_name %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>