<h1> Editing Student #<%= @student.id %></h1>
<%= semantic_form_for(@student) do |f| %>
<%= f.inputs do %>
    <%= f.input :first_name %>
		<%= f.input :last_name %>
<% end %>
<%= f.actions do %>
<%= f.action :submit, :as => :button %>
<% end %>
	
<% end %>